
version: 2

models:
    - name: mrt_order_daily_report
      description: "This model represents the order daily report"
      columns:
          - name: order_date
            description: "Date of the orders, primary key of the model"
            tests:
              - not_null
              - unique
              